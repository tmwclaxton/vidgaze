/**
 * Embed-capable platforms (PascalCase) — mirror `frontend_player` in config/platform_capabilities.php.
 * Update both when adding a web player implementation in PlayerStore.
 */
export const PLATFORM_EMBED_LABELS = Object.freeze([
    'YouTube',
    'Vimeo',
    'Twitch',
    'Dailymotion',
    'Rumble',
    'Odysee',
    'BitChute',
]);
