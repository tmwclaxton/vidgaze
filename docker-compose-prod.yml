version: '3'
services:
    laravel:
        build:
            context: .
            dockerfile: DockerfileProd
            args:
                WWWGROUP: '${WWWGROUP}'
        image: vidgaze-prod/app
        extra_hosts:
            - 'host.docker.internal:host-gateway'
        ports:
            - '${APP_PORT:-80}:80'
        environment:
            WWWUSER: '${WWWUSER}'
            LARAVEL_SAIL: 1
        networks:
            - sail
networks:
    sail:
        driver: bridge
